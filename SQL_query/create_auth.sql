CREATE TABLE AUTH(
    id  VARCHAR(10) NOT NULL,
    name VARCHAR(30) NOT NULL,
    credit INT NOT NULL,
    phone INT NOT NULL,
    visit INT,
    password VARCHAR(20) NOT NULL,
    PRIMARY KEY(id)
);